# The core service runs as `goblins-os`, while the desktop session bridge runs
# as the login user `goblin`. Keep the bridge socket outside `/run/user/1000`
# because that runtime directory is owner-only, and allow only members of the
# dedicated bridge group to traverse this path.
d /run/goblins-os-session 0770 goblin goblins-session-bridge -
