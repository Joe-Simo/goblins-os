[Unit]
# A first-boot Btrfs home migration is allowed to hide /var/home briefly while
# its original directory is held at a fixed seed path. Never start a graphical
# login unless the fail-closed setup service finished that critical section.
Requires=goblins-os-snapshots-setup.service
After=goblins-os-snapshots-setup.service
