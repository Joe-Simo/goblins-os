[Unit]
# Fedora bootc/ostree composefs boots expose / as an overlay. systemd-remount-fs
# cannot reconfigure that mount; ostree-remount.service handles the real sysroot.
ConditionKernelCommandLine=!ostree
